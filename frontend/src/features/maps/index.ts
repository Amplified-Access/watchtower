// Map data (points, summaries, report details) comes from the Go backend's
// /map endpoints via the `map` tRPC router; see src/lib/api/map.ts.
export { generateIncidentTypeSlug } from "./application/use-cases/generate-incident-type-slug";
